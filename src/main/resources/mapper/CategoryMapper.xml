<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.CategoryMapper">
    <select id="selectAll" resultType="org.example.entity.CreditCategory">
        SELECT * FROM credit_category
    </select>

    <select id="selectActive" resultType="org.example.entity.CreditCategory">
        SELECT * FROM credit_category WHERE status = 1
    </select>

    <select id="selectByCategoryId" parameterType="String" resultType="org.example.entity.CreditCategory">
        SELECT * FROM credit_category WHERE category_id = #{categoryId}
    </select>

    <insert id="insert" parameterType="org.example.entity.CreditCategory">
        INSERT INTO credit_category (category_id, category_name, credit_value, remark, create_date, status)
        VALUES (#{categoryId}, #{categoryName}, #{creditValue}, #{remark}, #{createDate}, #{status})
    </insert>

    <update id="update" parameterType="org.example.entity.CreditCategory">
        UPDATE credit_category SET
                                   category_name = #{categoryName},
                                   credit_value = #{creditValue},
                                   remark = #{remark}
        WHERE category_id = #{categoryId}
    </update>

    <update id="updateStatus" parameterType="org.example.entity.CreditCategory">
        UPDATE credit_category SET status = #{status} WHERE category_id = #{categoryId}
    </update>
</mapper>